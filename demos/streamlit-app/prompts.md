```text
write a new streamlit app based on the code in #file:etl-cheatsheet.ipynb
```

```text
can you rewrite the streamlit app instead of using a data file from local filesystem, to allow a user to upload a csv file instead?
```